{
    // Connection strings for the application. You can add multiple
    // connection strings here, one for each database your application
    // needs to connect to.
    "ConnectionStrings": {
        "DefaultConnection": "Server=(localdb)\\mssqllocaldb;Database=ModTwo;Trusted_Connection=True;MultipleActiveResultSets=true"
    },
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    // A list of hosts that the application is allowed to run on.
    // The wildcard (*) means the application can run on any host.
    "AllowedHosts": "*"
    // Additional configuration...
}
